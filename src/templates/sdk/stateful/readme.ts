// Intentionally identical to the stateless template - see "Why stateless and
// stateful are identical" in CLAUDE.md. Edit behaviour in stateless/, not here.
export { getReadmeTemplate } from '../stateless/readme.js';
